-- Camera.lua
-- Made by SnerMorY

-- All cameras
local Cameras = script.Cameras

local Camera = {
	Camera1 = require(Cameras.Camera1),
	Camera2 = require(Cameras.Camera2),
	Camera3 = require(Cameras.Camera3),
}

return Camera
